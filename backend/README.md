# Robust RAG Backend

FastAPI API、SQLAlchemy/Alembic、LocalFileStorage、Celery Worker 与 Robust RAG 领域实现。完整项目说明见仓库根目录的 `README.md`。

常用命令从仓库根目录执行：

```bash
make migrate
make api
make worker
make migration-check
```
